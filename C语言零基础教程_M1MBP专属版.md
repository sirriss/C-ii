# C语言零基础从入门到实战 - M1 MacBook Pro专属版

> **专为Apple Silicon打造的C语言学习指南**  
> 从环境搭建到实战项目，手把手教你掌握C语言编程

---

## 第1章：C语言介绍 + M1 MBP环境搭建

### 1.1 什么是C语言？

C语言是一种通用的编程语言，由Dennis Ritchie于1972年在贝尔实验室开发。它被称为"编程语言之母"，因为许多现代编程语言（如C++、Java、Objective-C、Swift）都受到了C语言的影响。

**为什么选择C语言？**

- ✅ **性能卓越**：直接操作内存，运行速度快
- ✅ **应用广泛**：操作系统、嵌入式系统、游戏开发等
- ✅ **基础语言**：学习C语言后再学其他语言会事半功倍
- ✅ **跨平台**：一份代码可以在不同系统上运行

### 1.2 M1 MacBook Pro开发环境搭建

在M1芯片上搭建C语言开发环境需要特别注意ARM架构的兼容性。下面我们将一步步配置完整的开发环境。

#### 步骤1：安装Homebrew（ARM版）

Homebrew是macOS上的包管理器，可以帮助我们安装各种开发工具。

```bash
# 在终端中执行以下命令安装Homebrew
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"

# 安装完成后，将Homebrew添加到PATH
# 对于M1芯片，Homebrew的默认路径是 /opt/homebrew
echo 'eval "$(/opt/homebrew/bin/brew shellenv)"' >> ~/.zshrc
source ~/.zshrc

# 验证安装
brew --version
```

**⚠️ M1专属提示**：Intel Mac的Homebrew路径是 `/usr/local/bin/brew`，而M1芯片的路径是 `/opt/homebrew`，不要混淆！

#### 步骤2：安装GCC编译器

虽然macOS自带Clang编译器，但安装GCC可以让我们获得更标准的C语言编译体验。

```bash
# 使用Homebrew安装GCC
brew install gcc

# 安装完成后，验证GCC版本
gcc-14 --version  # 注意：版本号可能不同，使用Tab键补全查看

# 为了方便使用，我们可以创建别名
echo 'alias gcc="gcc-14"' >> ~/.zshrc
source ~/.zshrc
```

#### 步骤3：安装VS Code（Apple Silicon版）

1. 访问 [VS Code官网](https://code.visualstudio.com/)
2. 下载 **Apple Silicon** 版本（不是Universal版本）
3. 安装并打开VS Code

#### 步骤4：安装VS Code插件

在VS Code中安装以下插件：

1. **C/C++** - Microsoft官方插件（搜索"C/C++"，选择Microsoft发布的）
2. **Code Runner** - 快速运行代码
3. **C/C++ Snippets** - 代码片段提示

**⚠️ 注意**：确保安装的插件版本标注有"Apple Silicon"或"ARM64"支持。

#### 步骤5：配置VS Code调试环境

创建配置文件以便在VS Code中编译和调试C程序：

**创建 `.vscode/tasks.json` 文件：**

```json
{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "gcc build active file",
            "type": "shell",
            "command": "/opt/homebrew/bin/gcc-14",
            "args": [
                "-g",
                "${file}",
                "-o",
                "${fileDirname}/${fileBasenameNoExtension}"
            ],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": ["$gcc"],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
```

**创建 `.vscode/launch.json` 文件：**

```json
{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "gcc build and debug active file",
            "type": "cppdbg",
            "request": "launch",
            "program": "${fileDirname}/${fileBasenameNoExtension}",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "environment": [],
            "externalConsole": false,
            "MIMode": "lldb",
            "preLaunchTask": "gcc build active file",
            "setupCommands": [
                {
                    "description": "Enable pretty-printing for gdb",
                    "text": "-enable-pretty-printing",
                    "ignoreFailures": true
                }
            ]
        }
    ]
}
```

#### 步骤6：测试环境

创建一个简单的测试文件 `test.c`：

```c
#include <stdio.h>

int main() {
    printf("Hello M1 Mac!\n");
    return 0;
}
```

在终端中编译运行：

```bash
# 使用gcc编译
gcc-14 test.c -o test

# 运行程序
./test
```

如果看到输出 "Hello M1 Mac!"，说明环境配置成功！

### 1.3 常见问题解决

**问题1：command not found: gcc**

```bash
# 解决方案：检查gcc路径
which gcc-14
# 输出应为：/opt/homebrew/bin/gcc-14

# 如果没有输出，说明gcc未正确安装
brew reinstall gcc
```

**问题2：找不到头文件**

```bash
# 安装Xcode命令行工具
xcode-select --install
```

**问题3：VS Code调试报错**

```bash
# 检查tasks.json中的gcc路径是否正确
# 应该是：/opt/homebrew/bin/gcc-14
```

### 1.4 课后练习

1. 在你的M1 MacBook Pro上成功配置开发环境
2. 创建一个简单的C程序，输出 "My first C program on M1!"
3. 分别在终端和VS Code中编译运行该程序

---

## 第2章：第一个C程序 - Hello World

### 2.1 创建第一个C程序

让我们创建经典的 "Hello World" 程序，这是学习任何编程语言的第一步。

**创建文件 `hello.c`：**

```c
// hello.c
// 这是我的第一个C语言程序

#include <stdio.h>  // 引入标准输入输出库

int main() {  // 主函数，程序的入口点
    printf("Hello, World!\n");  // 输出Hello, World!
    return 0;  // 返回0，表示程序正常结束
}
```

### 2.2 代码结构详解

让我们逐行分析这个程序：

**第1行：`#include <stdio.h>`**
- `#include` 是预处理指令，用于包含头文件
- `stdio.h` 是标准输入输出库的头文件
- 这个库提供了输入输出功能（如printf函数）

**第3行：`int main()`**
- `main()` 是主函数，每个C程序都必须有
- 程序从这里开始执行
- `int` 表示函数返回一个整数

**第4行：`{`**
- 函数体的开始

**第5行：`printf("Hello, World!\n");`**
- `printf()` 是输出函数，用于在屏幕上显示文字
- `"Hello, World!\n"` 是要输出的字符串
- `\n` 是换行符
- 语句以分号 `;` 结尾

**第6行：`return 0;`**
- 返回0，表示程序正常结束
- 这是良好的编程习惯

**第7行：`}`**
- 函数体的结束

### 2.3 在M1终端编译运行

打开终端，进入保存 `hello.c` 的目录：

```bash
# 使用cd命令进入目录
cd /Users/yourname/Documents/C_Learning  # 替换为你的实际路径

# 编译程序
gcc-14 hello.c -o hello

# 运行程序
./hello
```

**输出结果：**
```
Hello, World!
```

### 2.4 编译命令详解

**`gcc-14 hello.c -o hello`**

- `gcc-14`：使用GCC编译器（M1 ARM版本）
- `hello.c`：源文件名称
- `-o hello`：指定输出的可执行文件名
- 如果不使用 `-o`，默认会生成 `a.out`

**其他常用编译选项：**

```bash
# 添加调试信息
gcc-14 -g hello.c -o hello

# 显示所有警告
gcc-14 -Wall hello.c -o hello

# 优化代码
gcc-14 -O2 hello.c -o hello
```

### 2.5 M1下的常见问题

**问题1："command not found: gcc"**

```bash
# 解决方案：
# 方法1：使用完整路径
/opt/homebrew/bin/gcc-14 hello.c -o hello

# 方法2：创建别名（推荐）
echo 'alias gcc="gcc-14"' >> ~/.zshrc
source ~/.zshrc

# 现在可以简写为：
gcc hello.c -o hello
```

**问题2："No such file or directory"**

```bash
# 检查文件路径是否正确
ls -la  # 查看当前目录的文件

# 确保你在正确的目录下
cd /path/to/your/c/files
```

**问题3："Permission denied"**

```bash
# 给文件添加执行权限
chmod +x hello

# 或者删除后重新编译
rm hello
gcc-14 hello.c -o hello
```

### 2.6 在VS Code中运行

1. 在VS Code中打开 `hello.c` 文件
2. 按 `F5` 或点击 "Run → Start Debugging"
3. 程序将在终端中运行并显示输出

### 2.7 课后练习

**练习1：修改Hello World**
```c
// 修改程序，输出以下内容：
// Hello, C language!
// I am learning on my M1 Mac!
```

**练习2：多行输出**
```c
// 编写程序输出以下图案：
// *****
//  ***
//   *
```

**练习3：个人信息**
```c
// 编写程序输出你的姓名、年龄和使用的设备
// 例如：
// Name: John
// Age: 25
// Device: MacBook Pro M1
```

**练习答案：**

**练习1答案：**
```c
#include <stdio.h>

int main() {
    printf("Hello, C language!\n");
    printf("I am learning on my M1 Mac!\n");
    return 0;
}
```

**练习2答案：**
```c
#include <stdio.h>

int main() {
    printf("*****\n");
    printf(" ***\n");
    printf("  *\n");
    return 0;
}
```

**练习3答案：**
```c
#include <stdio.h>

int main() {
    printf("Name: Your Name\n");
    printf("Age: Your Age\n");
    printf("Device: MacBook Pro M1\n");
    return 0;
}
```

---

## 第3章：变量与数据类型

### 3.1 什么是变量？

变量是用来存储数据的容器。可以把变量想象成一个盒子，每个盒子都有名字，里面装着不同类型的数据。

### 3.2 C语言的数据类型

C语言提供了多种数据类型来存储不同类型的数据：

| 数据类型 | 含义 | 在M1下的内存占用 | 取值范围 |
|---------|------|-----------------|---------|
| `char` | 字符型 | 1字节 | -128 到 127 |
| `int` | 整型 | 4字节 | -2,147,483,648 到 2,147,483,647 |
| `float` | 单精度浮点型 | 4字节 | 约 ±3.4 × 10^38 |
| `double` | 双精度浮点型 | 8字节 | 约 ±1.7 × 10^308 |
| `short` | 短整型 | 2字节 | -32,768 到 32,767 |
| `long` | 长整型 | 8字节 | 约 ±9.2 × 10^18 |

**⚠️ M1架构提示**：在Apple Silicon上，`long`类型占用8字节，而在Intel架构上通常占用4字节。这是ARM架构的特点。

### 3.3 变量的声明和使用

**基本语法：**
```c
data_type variable_name = value;
```

**示例代码：**

```c
#include <stdio.h>

int main() {
    // 声明并初始化变量
    int age = 25;                    // 整数
    float height = 1.75;            // 浮点数
    char grade = 'A';               // 字符
    double pi = 3.1415926;          // 双精度浮点数
    
    // 使用变量
    printf("年龄: %d\n", age);
    printf("身高: %.2f 米\n", height);
    printf("成绩: %c\n", grade);
    printf("圆周率: %lf\n", pi);
    
    return 0;
}
```

### 3.4 格式说明符

`printf()`函数使用格式说明符来输出不同类型的数据：

| 格式说明符 | 作用 |
|-----------|------|
| `%d` 或 `%i` | 输出整数 |
| `%f` | 输出浮点数 |
| `%c` | 输出字符 |
| `%lf` | 输出双精度浮点数 |
| `%s` | 输出字符串 |
| `%.2f` | 输出保留2位小数的浮点数 |

### 3.5 变量的命名规则

1. **只能包含**：字母（a-z, A-Z）、数字（0-9）和下划线（_）
2. **不能以数字开头**：`int 2age;` 是错误的
3. **不能使用关键字**：如 `int`、`float` 等
4. **区分大小写**：`Age` 和 `age` 是不同的变量
5. **见名知意**：使用有意义的名字，如 `studentAge` 而不是 `a`

**良好的命名示例：**
```c
int student_age;
float average_score;
char user_name;
int totalCount;
```

**不好的命名示例：**
```c
int a;           // 没有意义
float 2height;   // 以数字开头
char my-name;    // 包含非法字符 -
int float;       // 使用关键字
```

### 3.6 常量

常量是一旦定义就不能改变的值。

**使用 `#define` 定义常量：**
```c
#include <stdio.h>

#define PI 3.14159    // 定义圆周率常量
#define MAX_AGE 150   // 定义最大年龄常量

int main() {
    printf("圆周率: %f\n", PI);
    printf("最大年龄: %d\n", MAX_AGE);
    return 0;
}
```

**使用 `const` 关键字：**
```c
#include <stdio.h>

int main() {
    const int max_score = 100;      // 定义常量
    const float tax_rate = 0.08;    // 税率
    
    printf("满分: %d\n", max_score);
    printf("税率: %.2f\n", tax_rate);
    
    // max_score = 120;  // 错误！不能修改常量
    
    return 0;
}
```

### 3.7 类型转换

**自动类型转换（隐式转换）：**
```c
#include <stdio.h>

int main() {
    int a = 10;
    float b = a;  // 整数自动转换为浮点数
    
    printf("b = %f\n", b);  // 输出：b = 10.000000
    
    return 0;
}
```

**强制类型转换（显式转换）：**
```c
#include <stdio.h>

int main() {
    float a = 10.75;
    int b = (int)a;  // 强制转换为整数
    
    printf("a = %f, b = %d\n", a, b);  // 输出：a = 10.750000, b = 10
    
    // 另一个例子：整数除法转换为浮点除法
    int c = 5;
    int d = 2;
    float result1 = c / d;           // 结果：2.000000（整数除法）
    float result2 = (float)c / d;    // 结果：2.500000（浮点除法）
    
    printf("result1 = %f\n", result1);
    printf("result2 = %f\n", result2);
    
    return 0;
}
```

### 3.8 sizeof运算符

`sizeof` 运算符可以查看数据类型或变量在内存中占用的字节数：

```c
#include <stdio.h>

int main() {
    // 查看基本数据类型的大小
    printf("char 占用: %lu 字节\n", sizeof(char));
    printf("int 占用: %lu 字节\n", sizeof(int));
    printf("float 占用: %lu 字节\n", sizeof(float));
    printf("double 占用: %lu 字节\n", sizeof(double));
    printf("long 占用: %lu 字节\n", sizeof(long));
    
    // 查看变量的大小
    int age = 25;
    printf("age 变量占用: %lu 字节\n", sizeof(age));
    
    return 0;
}
```

**在M1 MacBook Pro上的输出示例：**
```
char 占用: 1 字节
int 占用: 4 字节
float 占用: 4 字节
double 占用: 8 字节
long 占用: 8 字节
age 变量占用: 4 字节
```

### 3.9 实战案例

**案例：学生信息管理系统（简化版）**

```c
#include <stdio.h>

int main() {
    // 学生信息
    char name[50] = "张三";
    int age = 20;
    float height = 1.80;
    double weight = 70.5;
    char grade = 'A';
    int score = 95;
    
    // 输出学生信息
    printf("=== 学生信息 ===\n");
    printf("姓名: %s\n", name);
    printf("年龄: %d 岁\n", age);
    printf("身高: %.2f 米\n", height);
    printf("体重: %.1f 公斤\n", weight);
    printf("等级: %c\n", grade);
    printf("成绩: %d 分\n", score);
    
    // 计算BMI
    double bmi = weight / (height * height);
    printf("BMI: %.2f\n", bmi);
    
    return 0;
}
```

### 3.10 课后练习

**练习1：变量声明**
```c
// 声明以下变量并赋值：
// 1. 你的年龄（整数）
// 2. 你的身高（浮点数）
// 3. 你的成绩等级（字符）
// 4. 圆周率（双精度浮点数，使用3.14159）
// 然后使用printf输出这些变量
```

**练习2：温度转换**
```c
// 将摄氏温度转换为华氏温度
// 公式：华氏度 = 摄氏度 × 9/5 + 32
// 声明一个摄氏温度变量，计算并输出对应的华氏温度
```

**练习3：圆的计算**
```c
// 已知圆的半径，计算圆的周长和面积
// 周长公式：2 × π × r
// 面积公式：π × r × r
// 使用const定义π的值
```

**练习答案：**

**练习1答案：**
```c
#include <stdio.h>

int main() {
    int age = 25;
    float height = 1.75;
    char grade = 'B';
    double pi = 3.14159;
    
    printf("年龄: %d\n", age);
    printf("身高: %.2f\n", height);
    printf("成绩: %c\n", grade);
    printf("圆周率: %lf\n", pi);
    
    return 0;
}
```

**练习2答案：**
```c
#include <stdio.h>

int main() {
    float celsius = 30.0;
    float fahrenheit = celsius * 9.0 / 5.0 + 32;
    
    printf("摄氏温度: %.1f°C\n", celsius);
    printf("华氏温度: %.1f°F\n", fahrenheit);
    
    return 0;
}
```

**练习3答案：**
```c
#include <stdio.h>

int main() {
    const double PI = 3.14159;
    double radius = 5.0;
    
    double circumference = 2 * PI * radius;
    double area = PI * radius * radius;
    
    printf("半径: %.2f\n", radius);
    printf("周长: %.2f\n", circumference);
    printf("面积: %.2f\n", area);
    
    return 0;
}
```

---

## 第4章：运算符与表达式

### 4.1 什么是运算符？

运算符是用于执行各种操作的符号。C语言提供了丰富的运算符来处理数据。

### 4.2 算术运算符

用于执行基本的数学运算：

| 运算符 | 含义 | 示例 | 结果 |
|--------|------|------|------|
| `+` | 加法 | `5 + 3` | 8 |
| `-` | 减法 | `5 - 3` | 2 |
| `*` | 乘法 | `5 * 3` | 15 |
| `/` | 除法 | `5 / 2` | 2（整数除法） |
| `%` | 取余 | `5 % 2` | 1 |
| `++` | 自增 | `a++` | a的值增加1 |
| `--` | 自减 | `a--` | a的值减少1 |

**示例代码：**

```c
#include <stdio.h>

int main() {
    int a = 10, b = 3;
    
    // 算术运算
    printf("a + b = %d\n", a + b);  // 输出：13
    printf("a - b = %d\n", a - b);  // 输出：7
    printf("a * b = %d\n", a * b);  // 输出：30
    printf("a / b = %d\n", a / b);  // 输出：3（整数除法）
    printf("a %% b = %d\n", a % b);  // 输出：1
    
    // 自增和自减
    printf("\n自增和自减：\n");
    int x = 5;
    printf("x = %d\n", x);      // 输出：5
    printf("x++ = %d\n", x++);  // 输出：5（先使用，后增加）
    printf("x = %d\n", x);      // 输出：6
    
    int y = 5;
    printf("y = %d\n", y);      // 输出：5
    printf("++y = %d\n", ++y);  // 输出：6（先增加，后使用）
    printf("y = %d\n", y);      // 输出：6
    
    return 0;
}
```

**重要提示：**
- `++` 和 `--` 有前置和后置之分
- 前置：先增减，后使用（`++a`）
- 后置：先使用，后增减（`a++`）

### 4.3 关系运算符

用于比较两个值，结果为真（1）或假（0）：

| 运算符 | 含义 | 示例 | 结果 |
|--------|------|------|------|
| `==` | 等于 | `5 == 3` | 0（假） |
| `!=` | 不等于 | `5 != 3` | 1（真） |
| `>` | 大于 | `5 > 3` | 1（真） |
| `<` | 小于 | `5 < 3` | 0（假） |
| `>=` | 大于等于 | `5 >= 5` | 1（真） |
| `<=` | 小于等于 | `5 <= 3` | 0（假） |

**示例代码：**
```c
#include <stdio.h>

int main() {
    int a = 5, b = 3;
    
    printf("a == b: %d\n", a == b);  // 输出：0
    printf("a != b: %d\n", a != b);  // 输出：1
    printf("a > b: %d\n", a > b);    // 输出：1
    printf("a < b: %d\n", a < b);    // 输出：0
    printf("a >= b: %d\n", a >= b);  // 输出：1
    printf("a <= b: %d\n", a <= b);  // 输出：0
    
    return 0;
}
```

### 4.4 逻辑运算符

用于组合多个条件：

| 运算符 | 含义 | 示例 | 结果 |
|--------|------|------|------|
| `&&` | 逻辑与 | `a > 0 && b > 0` | 两个都为真才为真 |
| `||` | 逻辑或 | `a > 0 || b > 0` | 有一个为真就为真 |
| `!` | 逻辑非 | `!(a > 0)` | 真变假，假变真 |

**逻辑运算真值表：**

| A | B | A && B | A || B | !A |
|---|---|--------|--------|----|
| 真 | 真 | 真 | 真 | 假 |
| 真 | 假 | 假 | 真 | 假 |
| 假 | 真 | 假 | 真 | 真 |
| 假 | 假 | 假 | 假 | 真 |

**示例代码：**
```c
#include <stdio.h>

int main() {
    int a = 5, b = -2, c = 0;
    
    // 逻辑与
    printf("a > 0 && b > 0: %d\n", a > 0 && b > 0);  // 输出：0（假）
    printf("a > 0 && b < 0: %d\n", a > 0 && b < 0);  // 输出：1（真）
    
    // 逻辑或
    printf("a > 0 || b > 0: %d\n", a > 0 || b > 0);  // 输出：1（真）
    printf("b > 0 || c > 0: %d\n", b > 0 || c > 0);  // 输出：0（假）
    
    // 逻辑非
    printf("!(a > 0): %d\n", !(a > 0));  // 输出：0（假）
    printf("!(b > 0): %d\n", !(b > 0));  // 输出：1（真）
    
    return 0;
}
```

### 4.5 赋值运算符

用于给变量赋值：

| 运算符 | 示例 | 等价于 |
|--------|------|--------|
| `=` | `a = 5` | `a = 5` |
| `+=` | `a += 5` | `a = a + 5` |
| `-=` | `a -= 5` | `a = a - 5` |
| `*=` | `a *= 5` | `a = a * 5` |
| `/=` | `a /= 5` | `a = a / 5` |
| `%=` | `a %= 5` | `a = a % 5` |

**示例代码：**
```c
#include <stdio.h>

int main() {
    int a = 10;
    
    printf("初始值: a = %d\n", a);  // 输出：10
    
    a += 5;  // a = a + 5
    printf("a += 5 后: a = %d\n", a);  // 输出：15
    
    a -= 3;  // a = a - 3
    printf("a -= 3 后: a = %d\n", a);  // 输出：12
    
    a *= 2;  // a = a * 2
    printf("a *= 2 后: a = %d\n", a);  // 输出：24
    
    a /= 4;  // a = a / 4
    printf("a /= 4 后: a = %d\n", a);  // 输出：6
    
    a %= 4;  // a = a % 4
    printf("a %%= 4 后: a = %d\n", a);  // 输出：2
    
    return 0;
}
```

### 4.6 运算符优先级

当表达式中有多个运算符时，按照优先级进行计算：

| 优先级 | 运算符 | 结合性 |
|--------|--------|--------|
| 1 | `()` | 从左到右 |
| 2 | `++`, `--`, `!` | 从右到左 |
| 3 | `*`, `/`, `%` | 从左到右 |
| 4 | `+`, `-` | 从左到右 |
| 5 | `<`, `<=`, `>`, `>=` | 从左到右 |
| 6 | `==`, `!=` | 从左到右 |
| 7 | `&&` | 从左到右 |
| 8 | `||` | 从左到右 |
| 9 | `=` | 从右到左 |

**示例：**
```c
#include <stdio.h>

int main() {
    int result;
    
    // 例1：优先级
    result = 5 + 3 * 2;  // 先乘除后加减
    printf("5 + 3 * 2 = %d\n", result);  // 输出：11
    
    // 例2：使用括号改变优先级
    result = (5 + 3) * 2;
    printf("(5 + 3) * 2 = %d\n", result);  // 输出：16
    
    // 例3：逻辑运算优先级
    int a = 5, b = 3, c = 8;
    result = a > b && c > a;  // && 优先级低于关系运算符
    printf("a > b && c > a: %d\n", result);  // 输出：1
    
    return 0;
}
```

### 4.7 实战案例：简单计算器

```c
#include <stdio.h>

int main() {
    double num1, num2;
    char operator;
    
    printf("简单计算器\n");
    printf("请输入第一个数字: ");
    scanf("%lf", &num1);
    
    printf("请输入运算符(+, -, *, /): ");
    scanf(" %c", &operator);  // 注意前面的空格，用于跳过换行符
    
    printf("请输入第二个数字: ");
    scanf("%lf", &num2);
    
    // 计算并显示结果
    switch(operator) {
        case '+':
            printf("%.2f + %.2f = %.2f\n", num1, num2, num1 + num2);
            break;
        case '-':
            printf("%.2f - %.2f = %.2f\n", num1, num2, num1 - num2);
            break;
        case '*':
            printf("%.2f × %.2f = %.2f\n", num1, num2, num1 * num2);
            break;
        case '/':
            if(num2 != 0) {
                printf("%.2f ÷ %.2f = %.2f\n", num1, num2, num1 / num2);
            } else {
                printf("错误：除数不能为0！\n");
            }
            break;
        default:
            printf("错误：无效的运算符！\n");
    }
    
    return 0;
}
```

### 4.8 M1终端运行练习

将上述代码保存为 `calculator.c`，在终端中运行：

```bash
# 编译
gcc-14 calculator.c -o calculator

# 运行
./calculator

# 示例输出：
简单计算器
请输入第一个数字: 15.5
请输入运算符(+, -, *, /): *
请输入第二个数字: 2
15.50 × 2.00 = 31.00
```

### 4.9 课后练习

**练习1：表达式计算**
```c
// 计算以下表达式的值：
// 1. 10 + 20 * 3 / 4 - 5
// 2. (15 + 5) * 2 / 4 % 3
// 3. 8 > 5 && 3 < 9 || 10 == 10
```

**练习2：温度转换增强版**
```c
// 在上一章的温度转换程序基础上，添加以下功能：
// 1. 判断温度是否高于37度（发烧）
// 2. 判断温度是否在36-37度之间（正常体温）
// 使用关系运算符和逻辑运算符
```

**练习3：奇偶性判断**
```c
// 输入一个整数，判断它是奇数还是偶数
// 提示：使用 % 运算符
```

**练习答案：**

**练习1答案：**
```c
#include <stdio.h>

int main() {
    int result1 = 10 + 20 * 3 / 4 - 5;
    int result2 = (15 + 5) * 2 / 4 % 3;
    int result3 = 8 > 5 && 3 < 9 || 10 == 10;
    
    printf("10 + 20 * 3 / 4 - 5 = %d\n", result1);      // 输出：20
    printf("(15 + 5) * 2 / 4 %% 3 = %d\n", result2);    // 输出：1
    printf("8 > 5 && 3 < 9 || 10 == 10 = %d\n", result3); // 输出：1
    
    return 0;
}
```

**练习2答案：**
```c
#include <stdio.h>

int main() {
    float celsius;
    
    printf("请输入摄氏温度: ");
    scanf("%f", &celsius);
    
    float fahrenheit = celsius * 9.0 / 5.0 + 32;
    
    printf("%.1f°C = %.1f°F\n", celsius, fahrenheit);
    
    // 判断体温状态
    if(celsius > 37.0) {
        printf("警告：体温偏高，可能发烧！\n");
    } else if(celsius >= 36.0 && celsius <= 37.0) {
        printf("体温正常\n");
    } else {
        printf("体温偏低\n");
    }
    
    return 0;
}
```

**练习3答案：**
```c
#include <stdio.h>

int main() {
    int number;
    
    printf("请输入一个整数: ");
    scanf("%d", &number);
    
    if(number % 2 == 0) {
        printf("%d 是偶数\n", number);
    } else {
        printf("%d 是奇数\n", number);
    }
    
    return 0;
}
```

---

## 第5章：分支结构

### 5.1 什么是分支结构？

分支结构让程序能够根据不同条件执行不同的代码块。就像生活中的选择：如果下雨就带伞，否则不带伞。

### 5.2 if语句

**基本语法：**
```c
if(条件) {
    // 条件为真时执行的代码
}
```

**示例：**
```c
#include <stdio.h>

int main() {
    int age;
    
    printf("请输入你的年龄: ");
    scanf("%d", &age);
    
    if(age >= 18) {
        printf("你是成年人\n");
    }
    
    printf("程序结束\n");
    return 0;
}
```

### 5.3 if-else语句

**基本语法：**
```c
if(条件) {
    // 条件为真时执行的代码
} else {
    // 条件为假时执行的代码
}
```

**示例：**
```c
#include <stdio.h>

int main() {
    int age;
    
    printf("请输入你的年龄: ");
    scanf("%d", &age);
    
    if(age >= 18) {
        printf("你是成年人\n");
    } else {
        printf("你是未成年人\n");
    }
    
    return 0;
}
```

### 5.4 if-else if-else语句

用于多个条件的判断：

**基本语法：**
```c
if(条件1) {
    // 条件1为真时执行的代码
} else if(条件2) {
    // 条件2为真时执行的代码
} else if(条件3) {
    // 条件3为真时执行的代码
} else {
    // 所有条件都为假时执行的代码
}
```

**示例：成绩等级判断**
```c
#include <stdio.h>

int main() {
    int score;
    
    printf("请输入成绩(0-100): ");
    scanf("%d", &score);
    
    if(score >= 90) {
        printf("优秀 (A)\n");
    } else if(score >= 80) {
        printf("良好 (B)\n");
    } else if(score >= 70) {
        printf("中等 (C)\n");
    } else if(score >= 60) {
        printf("及格 (D)\n");
    } else {
        printf("不及格 (F)\n");
    }
    
    return 0;
}
```

### 5.5 switch语句

当需要判断一个变量等于多个不同值时，使用switch语句更清晰。

**基本语法：**
```c
switch(表达式) {
    case 常量1:
        // 代码块1
        break;
    case 常量2:
        // 代码块2
        break;
    default:
        // 默认代码块
}
```

**示例：星期几判断**
```c
#include <stdio.h>

int main() {
    int day;
    
    printf("请输入数字(1-7): ");
    scanf("%d", &day);
    
    switch(day) {
        case 1:
            printf("星期一\n");
            break;
        case 2:
            printf("星期二\n");
            break;
        case 3:
            printf("星期三\n");
            break;
        case 4:
            printf("星期四\n");
            break;
        case 5:
            printf("星期五\n");
            break;
        case 6:
            printf("星期六\n");
            break;
        case 7:
            printf("星期日\n");
            break;
        default:
            printf("无效输入！请输入1-7之间的数字\n");
    }
    
    return 0;
}
```

### 5.6 嵌套if语句

if语句中可以包含另一个if语句：

```c
#include <stdio.h>

int main() {
    int age;
    char gender;
    
    printf("请输入年龄: ");
    scanf("%d", &age);
    
    printf("请输入性别(M/F): ");
    scanf(" %c", &gender);  // 注意前面的空格
    
    if(age >= 18) {
        if(gender == 'M' || gender == 'm') {
            printf("成年男性\n");
        } else if(gender == 'F' || gender == 'f') {
            printf("成年女性\n");
        } else {
            printf("成年人\n");
        }
    } else {
        printf("未成年人\n");
    }
    
    return 0;
}
```

### 5.7 实战案例

**案例1：M1芯片温度监控**
```c
#include <stdio.h>

int main() {
    double temperature;
    
    printf("请输入M1芯片温度(°C): ");
    scanf("%lf", &temperature);
    
    if(temperature < 0) {
        printf("错误：温度不能为负数！\n");
    } else if(temperature < 30) {
        printf("温度正常 (%.1f°C)\n", temperature);
        printf("状态：优秀\n");
    } else if(temperature < 50) {
        printf("温度偏高 (%.1f°C)\n", temperature);
        printf("建议：关闭不必要的应用\n");
    } else if(temperature < 70) {
        printf("温度过高 (%.1f°C)\n", temperature);
        printf("警告：请让设备休息一会儿\n");
    } else {
        printf("温度危险 (%.1f°C)\n", temperature);
        printf("紧急：立即关闭设备！\n");
    }
    
    return 0;
}
```

**案例2：M1应用兼容性检测**
```c
#include <stdio.h>

int main() {
    char app_type;
    int is_optimized;
    
    printf("应用类型 (A=原生ARM, I=Intel, U=通用): ");
    scanf(" %c", &app_type);
    
    printf("是否针对M1优化 (1=是, 0=否): ");
    scanf("%d", &is_optimized);
    
    switch(app_type) {
        case 'A':
        case 'a':
            printf("✅ 原生ARM应用\n");
            printf("性能：最佳\n");
            break;
            
        case 'I':
        case 'i':
            if(is_optimized) {
                printf("⚠️ Intel应用（Rosetta转译）\n");
                printf("性能：良好\n");
            } else {
                printf("❌ Intel应用（未优化）\n");
                printf("性能：可能有问题\n");
            }
            break;
            
        case 'U':
        case 'u':
            printf("✅ 通用应用\n");
            if(is_optimized) {
                printf("性能：优秀\n");
            } else {
                printf("性能：良好\n");
            }
            break;
            
        default:
            printf("未知应用类型\n");
    }
    
    return 0;
}
```

**案例3：M1电池健康诊断**
```c
#include <stdio.h>

int main() {
    int battery_health;  // 电池健康度百分比
    int cycle_count;     // 充电循环次数
    
    printf("请输入电池健康度(0-100): ");
    scanf("%d", &battery_health);
    
    printf("请输入充电循环次数: ");
    scanf("%d", &cycle_count);
    
    // 电池健康评估
    if(battery_health >= 90) {
        printf("🔋 电池状态：优秀\n");
        printf("健康度：%d%%\n", battery_health);
    } else if(battery_health >= 80) {
        printf("🔋 电池状态：良好\n");
        printf("健康度：%d%%\n", battery_health);
    } else if(battery_health >= 70) {
        printf("🔋 电池状态：一般\n");
        printf("健康度：%d%%\n", battery_health);
        printf("建议：考虑更换电池\n");
    } else {
        printf("🔋 电池状态：较差\n");
        printf("健康度：%d%%\n", battery_health);
        printf("警告：强烈建议更换电池\n");
    }
    
    // 充电循环评估
    if(cycle_count < 500) {
        printf("充电循环：正常\n");
    } else if(cycle_count < 1000) {
        printf("充电循环：较多\n");
    } else {
        printf("充电循环：过多\n");
    }
    
    return 0;
}
```

### 5.8 M1下编译报错排查

**常见错误1：忘记break语句**
```c
// 错误代码
switch(day) {
    case 1:
        printf("星期一\n");
        // 忘记写break，会继续执行下面的case
    case 2:
        printf("星期二\n");
        break;
}

// 正确代码
switch(day) {
    case 1:
        printf("星期一\n");
        break;  // 加上break
    case 2:
        printf("星期二\n");
        break;
}
```

**常见错误2：条件判断使用赋值运算符**
```c
// 错误代码（容易混淆）
if(a = 5) {  // 这是赋值，不是比较
    printf("a等于5\n");
}

// 正确代码
if(a == 5) {  // 使用==比较
    printf("a等于5\n");
}
```

**常见错误3：scanf忘记取地址符&**
```c
// 错误代码
int age;
scanf("%d", age);  // 缺少&

// 正确代码
scanf("%d", &age);  // 加上&
```

### 5.9 课后练习

**练习1：月份天数判断**
```c
// 输入一个月份(1-12)，输出该月有多少天
// 考虑闰年（2月有29天）
// 闰年规则：能被4整除但不能被100整除，或者能被400整除
```

**练习2：简单ATM机**
```c
// 模拟ATM机操作：
// 1. 查询余额
// 2. 存款
// 3. 取款
// 4. 退出
// 使用switch语句实现
```

**练习3：BMI健康计算器**
```c
// 输入身高（米）和体重（公斤），计算BMI
// BMI = 体重 / (身高 × 身高)
// 根据BMI值判断健康状况：
// <18.5：偏瘦
// 18.5-24.9：正常
// 25-29.9：超重
// >=30：肥胖
```

**练习答案：**

**练习1答案：**
```c
#include <stdio.h>

int main() {
    int month, year;
    int days;
    
    printf("请输入年份: ");
    scanf("%d", &year);
    
    printf("请输入月份(1-12): ");
    scanf("%d", &month);
    
    switch(month) {
        case 1: case 3: case 5: case 7: case 8: case 10: case 12:
            days = 31;
            break;
        case 4: case 6: case 9: case 11:
            days = 30;
            break;
        case 2:
            // 判断闰年
            if((year % 4 == 0 && year % 100 != 0) || (year % 400 == 0)) {
                days = 29;
            } else {
                days = 28;
            }
            break;
        default:
            printf("无效的月份！\n");
            return 1;
    }
    
    printf("%d年%d月有%d天\n", year, month, days);
    
    return 0;
}
```

**练习2答案：**
```c
#include <stdio.h>

int main() {
    int choice;
    float balance = 1000.0;  // 初始余额
    float amount;
    
    do {
        printf("\n=== ATM机 ===\n");
        printf("1. 查询余额\n");
        printf("2. 存款\n");
        printf("3. 取款\n");
        printf("4. 退出\n");
        printf("请选择操作: ");
        scanf("%d", &choice);
        
        switch(choice) {
            case 1:
                printf("当前余额: ¥%.2f\n", balance);
                break;
                
            case 2:
                printf("请输入存款金额: ");
                scanf("%f", &amount);
                if(amount > 0) {
                    balance += amount;
                    printf("存款成功！\n");
                } else {
                    printf("无效金额！\n");
                }
                break;
                
            case 3:
                printf("请输入取款金额: ");
                scanf("%f", &amount);
                if(amount > 0 && amount <= balance) {
                    balance -= amount;
                    printf("取款成功！\n");
                } else if(amount > balance) {
                    printf("余额不足！\n");
                } else {
                    printf("无效金额！\n");
                }
                break;
                
            case 4:
                printf("感谢使用！再见！\n");
                break;
                
            default:
                printf("无效的选择！\n");
        }
    } while(choice != 4);
    
    return 0;
}
```

**练习3答案：**
```c
#include <stdio.h>

int main() {
    float height, weight, bmi;
    
    printf("请输入身高(米): ");
    scanf("%f", &height);
    
    printf("请输入体重(公斤): ");
    scanf("%f", &weight);
    
    if(height > 0) {
        bmi = weight / (height * height);
        
        printf("\nBMI指数: %.2f\n", bmi);
        
        if(bmi < 18.5) {
            printf("体重状态: 偏瘦\n");
            printf("建议: 适当增加营养\n");
        } else if(bmi < 25) {
            printf("体重状态: 正常\n");
            printf("建议: 保持良好生活习惯\n");
        } else if(bmi < 30) {
            printf("体重状态: 超重\n");
            printf("建议: 适当运动\n");
        } else {
            printf("体重状态: 肥胖\n");
            printf("建议: 咨询医生制定减重计划\n");
        }
    } else {
        printf("身高输入错误！\n");
    }
    
    return 0;
}
```

---

## 第6章：循环结构

### 6.1 什么是循环？

循环结构让程序能够重复执行某段代码。就像生活中的重复动作：每天吃饭、每周上课等。

### 6.2 for循环

**基本语法：**
```c
for(初始化; 条件; 更新) {
    // 循环体
}
```

**执行流程：**
1. 执行初始化语句
2. 检查条件是否为真
3. 如果为真，执行循环体
4. 执行更新语句
5. 回到第2步

**示例1：打印数字1-10**
```c
#include <stdio.h>

int main() {
    int i;
    
    for(i = 1; i <= 10; i++) {
        printf("%d ", i);
    }
    printf("\n");
    
    return 0;
}
```

**示例2：计算1到100的和**
```c
#include <stdio.h>

int main() {
    int sum = 0;
    int i;
    
    for(i = 1; i <= 100; i++) {
        sum += i;  // sum = sum + i
    }
    
    printf("1到100的和是: %d\n", sum);  // 输出：5050
    
    return 0;
}
```

### 6.3 while循环

**基本语法：**
```c
while(条件) {
    // 循环体
}
```

**示例：猜数字游戏**
```c
#include <stdio.h>
#include <stdlib.h>
#include <time.h>

int main() {
    int secret_number, guess, attempts = 0;
    
    // 生成随机数
    srand(time(NULL));
    secret_number = rand() % 100 + 1;  // 1-100之间的随机数
    
    printf("欢迎来到猜数字游戏！\n");
    printf("我已经想好了一个1-100之间的数字。\n");
    
    while(1) {  // 无限循环
        printf("请输入你的猜测: ");
        scanf("%d", &guess);
        attempts++;
        
        if(guess < secret_number) {
            printf("太小了！\n");
        } else if(guess > secret_number) {
            printf("太大了！\n");
        } else {
            printf("恭喜你，猜对了！\n");
            printf("你总共猜了%d次。\n", attempts);
            break;  // 跳出循环
        }
    }
    
    return 0;
}
```

### 6.4 do-while循环

**基本语法：**
```c
do {
    // 循环体
} while(条件);
```

**特点：** 至少执行一次循环体，然后再检查条件。

**示例：菜单选择**
```c
#include <stdio.h>

int main() {
    int choice;
    
    do {
        printf("\n=== 菜单 ===\n");
        printf("1. 开始游戏\n");
        printf("2. 设置\n");
        printf("3. 退出\n");
        printf("请选择: ");
        scanf("%d", &choice);
        
        switch(choice) {
            case 1:
                printf("游戏开始！\n");
                break;
            case 2:
                printf("进入设置...\n");
                break;
            case 3:
                printf("退出游戏\n");
                break;
            default:
                printf("无效选择！\n");
        }
    } while(choice != 3);
    
    return 0;
}
```

### 6.5 循环嵌套

循环中可以有循环：

**示例：九九乘法表**
```c
#include <stdio.h>

int main() {
    int i, j;
    
    printf("九九乘法表\n");
    printf("------------\n");
    
    for(i = 1; i <= 9; i++) {  // 外层循环控制行
        for(j = 1; j <= i; j++) {  // 内层循环控制列
            printf("%d × %d = %-2d  ", j, i, i * j);
        }
        printf("\n");  // 每行结束换行
    }
    
    return 0;
}
```

**输出：**
```
九九乘法表
------------
1 × 1 = 2   
1 × 2 = 2   2 × 2 = 4   
1 × 3 = 3   2 × 3 = 6   3 × 3 = 9   
...
1 × 9 = 9   2 × 9 = 18  3 × 9 = 27  ... 9 × 9 = 81  
```

### 6.6 循环控制语句

**break语句：** 立即跳出循环
```c
#include <stdio.h>

int main() {
    int i;
    
    for(i = 1; i <= 10; i++) {
        if(i == 5) {
            break;  // 当i等于5时跳出循环
        }
        printf("%d ", i);
    }
    printf("\n循环结束\n");
    
    return 0;
}
// 输出：1 2 3 4 
```

**continue语句：** 跳过本次循环，继续下一次
```c
#include <stdio.h>

int main() {
    int i;
    
    for(i = 1; i <= 10; i++) {
        if(i % 2 == 0) {  // 如果是偶数
            continue;     // 跳过本次循环
        }
        printf("%d ", i);
    }
    printf("\n");
    
    return 0;
}
// 输出：1 3 5 7 9 
```

### 6.7 实战案例

**案例1：计算阶乘**
```c
#include <stdio.h>

int main() {
    int n, i;
    long long factorial = 1;
    
    printf("请输入一个正整数: ");
    scanf("%d", &n);
    
    if(n < 0) {
        printf("错误：不能计算负数的阶乘\n");
    } else {
        for(i = 1; i <= n; i++) {
            factorial *= i;
        }
        printf("%d的阶乘是: %lld\n", n, factorial);
    }
    
    return 0;
}
```

**案例2：打印金字塔图案**
```c
#include <stdio.h>

int main() {
    int i, j, space, rows;
    
    printf("请输入金字塔的行数: ");
    scanf("%d", &rows);
    
    for(i = 1; i <= rows; i++) {
        // 打印空格
        for(space = 1; space <= rows - i; space++) {
            printf(" ");
        }
        
        // 打印星号
        for(j = 1; j <= 2 * i - 1; j++) {
            printf("*");
        }
        
        printf("\n");
    }
    
    return 0;
}
```

**案例3：M1性能测试（模拟）**
```c
#include <stdio.h>
#include <time.h>
#include <unistd.h>

int main() {
    int i;
    clock_t start, end;
    double cpu_time_used;
    
    printf("=== M1 MacBook Pro 性能测试 ===\n");
    printf("测试项目：整数运算性能\n");
    printf("测试次数：10000000\n\n");
    
    // 开始计时
    start = clock();
    
    // 执行大量计算
    long long sum = 0;
    for(i = 1; i <= 10000000; i++) {
        sum += i;
    }
    
    // 结束计时
    end = clock();
    cpu_time_used = ((double) (end - start)) / CLOCKS_PER_SEC;
    
    printf("计算完成！\n");
    printf("结果: %lld\n", sum);
    printf("用时: %.4f 秒\n", cpu_time_used);
    
    if(cpu_time_used < 0.1) {
        printf("\n🚀 性能评级：优秀\n");
        printf("你的M1芯片表现卓越！\n");
    } else if(cpu_time_used < 0.5) {
        printf("\n✅ 性能评级：良好\n");
        printf("M1性能稳定！\n");
    } else {
        printf("\n⚠️  性能评级：一般\n");
        printf("建议关闭其他应用\n");
    }
    
    return 0;
}
```

### 6.8 M1下运行无卡顿优化

在M1芯片上运行循环密集型程序时，可以利用以下优化：

```c
// 优化示例：使用寄存器变量
#include <stdio.h>

int main() {
    register int i;  // 建议编译器使用寄存器存储变量
    int sum = 0;
    
    // 使用更高效的循环方式
    for(i = 1; i <= 1000000; i++) {
        sum += i;
    }
    
    printf("Sum = %d\n", sum);
    return 0;
}
```

**编译优化选项：**
```bash
# 使用O2优化级别
gcc-14 -O2 your_program.c -o your_program

# 使用O3更高级别的优化（谨慎使用）
gcc-14 -O3 your_program.c -o your_program
```

### 6.9 课后练习

**练习1：素数判断**
```c
// 输入一个正整数，判断它是否为素数
// 素数：只能被1和它本身整除的数
// 提示：用循环检查是否能被2到sqrt(n)之间的数整除
```

**练习2：斐波那契数列**
```c
// 输出前n个斐波那契数列
// 斐波那契数列：1, 1, 2, 3, 5, 8, 13, ...
// 规律：每个数是前两个数的和
```

**练习3：数字游戏**
```c
// 输入一个数字，然后：
// 1. 如果是偶数，除以2
// 2. 如果是奇数，乘以3加1
// 3. 重复这个过程，直到数字变为1
// 输出每一步的结果
```

**练习答案：**

**练习1答案：**
```c
#include <stdio.h>
#include <math.h>

int main() {
    int n, i, is_prime = 1;
    
    printf("请输入一个正整数: ");
    scanf("%d", &n);
    
    if(n <= 1) {
        is_prime = 0;
    } else {
        for(i = 2; i <= sqrt(n); i++) {
            if(n % i == 0) {
                is_prime = 0;
                break;
            }
        }
    }
    
    if(is_prime) {
        printf("%d 是素数\n", n);
    } else {
        printf("%d 不是素数\n", n);
    }
    
    return 0;
}
```

**练习2答案：**
```c
#include <stdio.h>

int main() {
    int n, i;
    int first = 1, second = 1, next;
    
    printf("请输入要输出的斐波那契数列项数: ");
    scanf("%d", &n);
    
    printf("斐波那契数列前%d项:\n", n);
    
    for(i = 1; i <= n; i++) {
        if(i == 1) {
            printf("%d ", first);
        } else if(i == 2) {
            printf("%d ", second);
        } else {
            next = first + second;
            printf("%d ", next);
            first = second;
            second = next;
        }
    }
    printf("\n");
    
    return 0;
}
```

**练习3答案：**
```c
#include <stdio.h>

int main() {
    int n, step = 0;
    
    printf("请输入一个正整数: ");
    scanf("%d", &n);
    
    printf("计算过程:\n");
    printf("第%d步: %d\n", step, n);
    
    while(n != 1) {
        step++;
        if(n % 2 == 0) {
            n = n / 2;
        } else {
            n = n * 3 + 1;
        }
        printf("第%d步: %d\n", step, n);
    }
    
    printf("\n总共用了%d步\n", step);
    
    return 0;
}
```

---

## 第7章：数组

### 7.1 什么是数组？

数组是一组相同类型数据的集合，这些数据在内存中连续存储。可以把数组想象成一个储物柜，每个格子都存放着相同类型的物品。

### 7.2 一维数组

**声明语法：**
```c
data_type array_name[size];
```

**示例1：数组的基本使用**
```c
#include <stdio.h>

int main() {
    // 声明并初始化数组
    int numbers[5] = {10, 20, 30, 40, 50};
    
    // 访问数组元素
    printf("第一个元素: %d\n", numbers[0]);  // 输出：10
    printf("第三个元素: %d\n", numbers[2]);  // 输出：30
    
    // 修改数组元素
    numbers[1] = 25;
    printf("修改后的第二个元素: %d\n", numbers[1]);  // 输出：25
    
    // 遍历数组
    printf("数组所有元素: ");
    for(int i = 0; i < 5; i++) {
        printf("%d ", numbers[i]);
    }
    printf("\n");
    
    return 0;
}
```

**重要提示：**
- 数组索引从0开始，不是从1开始
- `numbers[0]` 是第一个元素
- `numbers[4]` 是第五个元素
- 访问 `numbers[5]` 会导致数组越界（错误！）

**示例2：计算数组平均值**
```c
#include <stdio.h>

int main() {
    int scores[10] = {85, 92, 78, 96, 88, 76, 95, 89, 91, 82};
    int sum = 0;
    float average;
    
    // 计算总分
    for(int i = 0; i < 10; i++) {
        sum += scores[i];
    }
    
    // 计算平均分
    average = (float)sum / 10;
    
    printf("总分: %d\n", sum);
    printf("平均分: %.2f\n", average);
    
    return 0;
}
```

### 7.3 数组的初始化

**完全初始化：**
```c
int numbers[5] = {1, 2, 3, 4, 5};
```

**部分初始化：**
```c
int numbers[5] = {1, 2};  // 其余元素自动初始化为0
// 结果：numbers = {1, 2, 0, 0, 0}
```

**全部初始化为0：**
```c
int numbers[5] = {0};  // 所有元素都是0
```

**省略数组大小：**
```c
int numbers[] = {1, 2, 3, 4, 5};  // 编译器自动计算大小为5
```

### 7.4 二维数组

二维数组可以看作表格，有行和列。

**声明语法：**
```c
data_type array_name[rows][columns];
```

**示例1：二维数组的基本使用**
```c
#include <stdio.h>

int main() {
    // 声明并初始化2行3列的二维数组
    int matrix[2][3] = {
        {1, 2, 3},
        {4, 5, 6}
    };
    
    // 访问元素
    printf("第一行第一列: %d\n", matrix[0][0]);  // 输出：1
    printf("第二行第三列: %d\n", matrix[1][2]);  // 输出：6
    
    // 修改元素
    matrix[0][1] = 10;
    
    // 遍历二维数组
    printf("矩阵所有元素:\n");
    for(int i = 0; i < 2; i++) {  // 遍历行
        for(int j = 0; j < 3; j++) {  // 遍历列
            printf("%d ", matrix[i][j]);
        }
        printf("\n");
    }
    
    return 0;
}
```

**示例2：学生成绩表**
```c
#include <stdio.h>

int main() {
    // 3个学生，4门课程的成绩
    float scores[3][4] = {
        {85, 90, 78, 92},  // 学生1
        {76, 88, 91, 85},  // 学生2
        {90, 85, 88, 94}   // 学生3
    };
    
    // 计算每个学生的平均分
    for(int i = 0; i < 3; i++) {
        float sum = 0;
        for(int j = 0; j < 4; j++) {
            sum += scores[i][j];
        }
        float average = sum / 4;
        printf("学生%d的平均分: %.2f\n", i + 1, average);
    }
    
    return 0;
}
```

### 7.5 字符数组和字符串

在C语言中，字符串是以空字符 `\0` 结尾的字符数组。

**字符数组初始化：**
```c
char str1[] = {'H', 'e', 'l', 'l', 'o', '\0'};
char str2[] = "Hello";  // 更简洁的方式
```

**字符串操作示例：**
```c
#include <stdio.h>
#include <string.h>

int main() {
    char name[50];
    
    // 输入字符串
    printf("请输入你的名字: ");
    scanf("%s", name);  // 注意：字符串不需要&
    
    // 输出字符串
    printf("你好，%s！\n", name);
    
    // 计算字符串长度
    printf("你的名字有%d个字母\n", strlen(name));
    
    // 字符串复制
    char greeting[100] = "Welcome, ";
    strcat(greeting, name);  // 连接字符串
    printf("%s\n", greeting);
    
    return 0;
}
```

### 7.6 冒泡排序

**算法原理：**
1. 比较相邻的两个元素
2. 如果顺序错误就交换它们
3. 对每一对相邻元素重复这个过程
4. 重复以上步骤，直到没有需要交换的

**代码实现：**
```c
#include <stdio.h>

int main() {
    int arr[] = {64, 34, 25, 12, 22, 11, 90};
    int n = sizeof(arr) / sizeof(arr[0]);
    int i, j, temp;
    
    printf("排序前的数组: ");
    for(i = 0; i < n; i++) {
        printf("%d ", arr[i]);
    }
    printf("\n");
    
    // 冒泡排序
    for(i = 0; i < n - 1; i++) {
        for(j = 0; j < n - i - 1; j++) {
            if(arr[j] > arr[j + 1]) {
                // 交换
                temp = arr[j];
                arr[j] = arr[j + 1];
                arr[j + 1] = temp;
            }
        }
        
        // 显示每轮排序结果
        printf("第%d轮排序后: ", i + 1);
        for(int k = 0; k < n; k++) {
            printf("%d ", arr[k]);
        }
        printf("\n");
    }
    
    printf("\n排序后的数组: ");
    for(i = 0; i < n; i++) {
        printf("%d ", arr[i]);
    }
    printf("\n");
    
    return 0;
}
```

### 7.7 M1下的数组内存布局

在M1 ARM架构下，数组在内存中是连续存储的：

```c
#include <stdio.h>

int main() {
    int arr[5] = {10, 20, 30, 40, 50};
    
    printf("M1 ARM架构下的数组内存布局:\n");
    printf("数组起始地址: %p\n", arr);
    
    for(int i = 0; i < 5; i++) {
        printf("arr[%d] 地址: %p, 值: %d\n", i, &arr[i], arr[i]);
    }
    
    printf("\n每个int类型占用: %lu 字节\n", sizeof(int));
    printf("数组总大小: %lu 字节\n", sizeof(arr));
    
    return 0;
}
```

**在M1上的输出示例：**
```
M1 ARM架构下的数组内存布局:
数组起始地址: 0x16fdff0a0
arr[0] 地址: 0x16fdff0a0, 值: 10
arr[1] 地址: 0x16fdff0a4, 值: 20
arr[2] 地址: 0x16fdff0a8, 值: 30
arr[3] 地址: 0x16fdff0ac, 值: 40
arr[4] 地址: 0x16fdff0b0, 值: 50

每个int类型占用: 4 字节
数组总大小: 20 字节
```

**观察：**
- 每个int占用4字节
- 数组元素地址相差4字节
- 内存是连续分配的

### 7.8 课后练习

**练习1：数组反转**
```c
// 输入一个整数数组，将其元素顺序反转
// 例如：输入[1, 2, 3, 4, 5]，输出[5, 4, 3, 2, 1]
```

**练习2：查找最大值和最小值**
```c
// 在数组中找出最大值和最小值及其位置
```

**练习3：矩阵转置**
```c
// 将一个2x3的矩阵转置为3x2的矩阵
// 转置：行变列，列变行
```

**练习答案：**

**练习1答案：**
```c
#include <stdio.h>

int main() {
    int arr[100], n, i, temp;
    
    printf("请输入数组大小: ");
    scanf("%d", &n);
    
    printf("请输入%d个整数:\n", n);
    for(i = 0; i < n; i++) {
        scanf("%d", &arr[i]);
    }
    
    printf("原数组: ");
    for(i = 0; i < n; i++) {
        printf("%d ", arr[i]);
    }
    printf("\n");
    
    // 反转数组
    for(i = 0; i < n / 2; i++) {
        temp = arr[i];
        arr[i] = arr[n - 1 - i];
        arr[n - 1 - i] = temp;
    }
    
    printf("反转后数组: ");
    for(i = 0; i < n; i++) {
        printf("%d ", arr[i]);
    }
    printf("\n");
    
    return 0;
}
```

**练习2答案：**
```c
#include <stdio.h>

int main() {
    int arr[100], n, i;
    int max, min, max_pos, min_pos;
    
    printf("请输入数组大小: ");
    scanf("%d", &n);
    
    printf("请输入%d个整数:\n", n);
    for(i = 0; i < n; i++) {
        scanf("%d", &arr[i]);
    }
    
    // 假设第一个元素既是最大值也是最小值
    max = min = arr[0];
    max_pos = min_pos = 0;
    
    for(i = 1; i < n; i++) {
        if(arr[i] > max) {
            max = arr[i];
            max_pos = i;
        }
        if(arr[i] < min) {
            min = arr[i];
            min_pos = i;
        }
    }
    
    printf("最大值: %d，位置: %d\n", max, max_pos);
    printf("最小值: %d，位置: %d\n", min, min_pos);
    
    return 0;
}
```

**练习3答案：**
```c
#include <stdio.h>

int main() {
    int matrix[2][3] = {
        {1, 2, 3},
        {4, 5, 6}
    };
    
    int transpose[3][2];
    int i, j;
    
    printf("原矩阵:\n");
    for(i = 0; i < 2; i++) {
        for(j = 0; j < 3; j++) {
            printf("%d ", matrix[i][j]);
        }
        printf("\n");
    }
    
    // 转置矩阵
    for(i = 0; i < 2; i++) {
        for(j = 0; j < 3; j++) {
            transpose[j][i] = matrix[i][j];
        }
    }
    
    printf("\n转置后的矩阵:\n");
    for(i = 0; i < 3; i++) {
        for(j = 0; j < 2; j++) {
            printf("%d ", transpose[i][j]);
        }
        printf("\n");
    }
    
    return 0;
}
```

---

## 第8章：函数

### 8.1 什么是函数？

函数是一段完成特定任务的代码块。使用函数可以让代码更模块化、更易读、更易维护。

**函数的好处：**
- ✅ 代码重用
- ✅ 模块化编程
- ✅ 易于测试和调试
- ✅ 提高代码可读性

### 8.2 函数的定义和调用

**函数定义语法：**
```c
返回类型 函数名(参数列表) {
    // 函数体
    return 返回值;
}
```

**示例1：简单的函数**
```c
#include <stdio.h>

// 函数声明（原型）
void say_hello();
int add(int a, int b);

int main() {
    // 调用函数
    say_hello();
    
    int result = add(5, 3);
    printf("5 + 3 = %d\n", result);
    
    return 0;
}

// 函数定义
void say_hello() {
    printf("Hello, World!\n");
}

int add(int a, int b) {
    int sum = a + b;
    return sum;
}
```

### 8.3 函数的参数传递

**值传递：**
```c
#include <stdio.h>

void modify_value(int x) {
    x = x * 2;
    printf("函数内: x = %d\n", x);
}

int main() {
    int num = 10;
    printf("调用前: num = %d\n", num);
    
    modify_value(num);
    
    printf("调用后: num = %d\n", num);  // num的值不变
    
    return 0;
}
```

**输出：**
```
调用前: num = 10
函数内: x = 20
调用后: num = 10
```

**重要概念：**
- C语言中，函数参数是值传递
- 函数内修改参数不会影响原来的变量
- 如果需要修改原变量，需要使用指针（见第9章）

### 8.4 函数的返回值

函数可以返回一个值给调用者：

```c
#include <stdio.h>

// 返回两个数中的最大值
int max(int a, int b) {
    if(a > b) {
        return a;
    } else {
        return b;
    }
}

// 返回三个数中的最大值
int max_three(int a, int b, int c) {
    int temp = max(a, b);  // 复用max函数
    return max(temp, c);
}

int main() {
    int a = 10, b = 20, c = 15;
    
    int maximum = max_three(a, b, c);
    printf("最大值是: %d\n", maximum);
    
    return 0;
}
```

### 8.5 递归函数

递归是函数调用自身的技术。

**递归的基本要素：**
1. **基本情况**：什么时候停止递归
2. **递归情况**：如何调用自身

**示例1：递归计算阶乘**
```c
#include <stdio.h>

long long factorial(int n) {
    // 基本情况
    if(n <= 1) {
        return 1;
    }
    // 递归情况
    else {
        return n * factorial(n - 1);
    }
}

int main() {
    int num;
    printf("请输入一个正整数: ");
    scanf("%d", &num);
    
    if(num < 0) {
        printf("错误：不能计算负数的阶乘\n");
    } else {
        long long result = factorial(num);
        printf("%d的阶乘是: %lld\n", num, result);
    }
    
    return 0;
}
```

**递归执行过程（以factorial(4)为例）：**
```
factorial(4)
= 4 * factorial(3)
= 4 * (3 * factorial(2))
= 4 * (3 * (2 * factorial(1)))
= 4 * (3 * (2 * 1))
= 24
```

**示例2：递归计算斐波那契数列**
```c
#include <stdio.h>

int fibonacci(int n) {
    if(n <= 1) {
        return n;
    } else {
        return fibonacci(n - 1) + fibonacci(n - 2);
    }
}

int main() {
    int n;
    printf("请输入要计算的斐波那契数列项数: ");
    scanf("%d", &n);
    
    printf("斐波那契数列前%d项:\n", n);
    for(int i = 0; i < n; i++) {
        printf("%d ", fibonacci(i));
    }
    printf("\n");
    
    return 0;
}
```

### 8.6 实战案例

**案例1：M1系统信息工具**
```c
#include <stdio.h>
#include <string.h>

// 显示系统欢迎信息
void show_welcome() {
    printf("=====================================\n");
    printf("   M1 MacBook Pro 系统信息工具\n");
    printf("=====================================\n\n");
}

// 显示CPU信息
void show_cpu_info() {
    printf("CPU信息:\n");
    printf("  型号: Apple M1 Pro\n");
    printf("  架构: ARM64\n");
    printf("  核心: 8核 (6性能核 + 2能效核)\n");
    printf("  频率: 3.2 GHz\n\n");
}

// 显示内存信息
void show_memory_info() {
    printf("内存信息:\n");
    printf("  总内存: 16 GB\n");
    printf("  类型: 统一内存\n");
    printf("  带宽: 200 GB/s\n\n");
}

// 显示存储信息
void show_storage_info() {
    printf("存储信息:\n");
    printf("  容量: 512 GB SSD\n");
    printf("  可用空间: 342 GB\n");
    printf("  格式: APFS\n\n");
}

// 计算并显示电池健康度
void show_battery_health(int cycle_count, int original_capacity, int current_capacity) {
    float health_percentage = (float)current_capacity / original_capacity * 100;
    
    printf("电池信息:\n");
    printf("  循环次数: %d\n", cycle_count);
    printf("  原始容量: %d mAh\n", original_capacity);
    printf("  当前容量: %d mAh\n", current_capacity);
    printf("  健康度: %.1f%%\n", health_percentage);
    
    if(health_percentage > 90) {
        printf("  状态: 优秀 🔋\n");
    } else if(health_percentage > 80) {
        printf("  状态: 良好 ✅\n");
    } else {
        printf("  状态: 需要关注 ⚠️\n");
    }
}

int main() {
    // 显示所有信息
    show_welcome();
    show_cpu_info();
    show_memory_info();
    show_storage_info();
    show_battery_health(245, 8740, 7890);
    
    return 0;
}
```

**案例2：数学工具函数库**
```c
#include <stdio.h>
#include <math.h>

// 计算两个数的最大公约数
gcd(int a, int b) {
    while(b != 0) {
        int temp = b;
        b = a % b;
        a = temp;
    }
    return a;
}

// 判断是否为素数
int is_prime(int n) {
    if(n <= 1) return 0;
    for(int i = 2; i <= sqrt(n); i++) {
        if(n % i == 0) return 0;
    }
    return 1;
}

// 计算数字的位数
int count_digits(int n) {
    int count = 0;
    if(n == 0) return 1;
    
    while(n != 0) {
        n = n / 10;
        count++;
    }
    return count;
}

// 反转数字
int reverse_number(int n) {
    int reversed = 0;
    while(n != 0) {
        reversed = reversed * 10 + n % 10;
        n = n / 10;
    }
    return reversed;
}

// 判断是否为回文数
int is_palindrome(int n) {
    return n == reverse_number(n);
}

int main() {
    int num1 = 36, num2 = 60;
    printf("GCD(%d, %d) = %d\n", num1, num2, gcd(num1, num2));
    
    int test_num = 12321;
    printf("%d 是%s回文数\n", test_num, is_palindrome(test_num) ? "" : "不");
    
    int prime_candidate = 97;
    printf("%d %s素数\n", prime_candidate, is_prime(prime_candidate) ? "是" : "不是");
    
    return 0;
}
```

### 8.7 M1终端运行演示

将函数代码保存为 `functions.c`，在终端中编译运行：

```bash
# 编译
gcc-14 functions.c -o functions

# 运行
./functions

# 示例输出
GCD(36, 60) = 12
12321 是回文数
97 是素数
```

### 8.8 函数的最佳实践

1. **函数命名**：使用有意义的名称，如 `calculate_average` 而不是 `calc`
2. **函数大小**：一个函数只做一件事，避免过大
3. **函数注释**：为函数添加说明
4. **避免全局变量**：尽量使用参数传递数据

**良好的函数示例：**
```c
// 计算矩形的面积
// 参数：width - 宽度，height - 高度
// 返回：矩形的面积
float calculate_rectangle_area(float width, float height) {
    return width * height;
}

// 判断年份是否为闰年
// 参数：year - 要判断的年份
// 返回：1表示是闰年，0表示不是闰年
int is_leap_year(int year) {
    return (year % 4 == 0 && year % 100 != 0) || (year % 400 == 0);
}
```

### 8.9 课后练习

**练习1：函数综合练习**
```c
// 编写以下函数：
// 1. 计算圆的面积和周长
// 2. 将摄氏温度转换为华氏温度
// 3. 判断一个年份是否是闰年
// 在主函数中调用这些函数
```

**练习2：递归练习**
```c
// 使用递归实现以下功能：
// 1. 计算一个数字的各位数字之和（如123 → 1+2+3=6）
// 2. 计算a的b次方（a^b）
```

**练习3：数组和函数结合**
```c
// 编写以下函数：
// 1. 计算数组所有元素的和
// 2. 找出数组中的最大值和最小值
// 3. 对数组进行排序（使用冒泡排序）
// 在主函数中测试这些函数
```

**练习答案：**

**练习1答案：**
```c
#include <stdio.h>
#define PI 3.14159

float circle_area(float radius) {
    return PI * radius * radius;
}

float circle_circumference(float radius) {
    return 2 * PI * radius;
}

float celsius_to_fahrenheit(float celsius) {
    return celsius * 9.0 / 5.0 + 32;
}

int is_leap_year(int year) {
    return (year % 4 == 0 && year % 100 != 0) || (year % 400 == 0);
}

int main() {
    float radius = 5.0;
    printf("半径为%.1f的圆:\n", radius);
    printf("面积: %.2f\n", circle_area(radius));
    printf("周长: %.2f\n\n", circle_circumference(radius));
    
    float temp = 25.0;
    printf("%.1f°C = %.1f°F\n\n", temp, celsius_to_fahrenheit(temp));
    
    int year = 2024;
    printf("%d年%s闰年\n", year, is_leap_year(year) ? "是" : "不是");
    
    return 0;
}
```

**练习2答案：**
```c
#include <stdio.h>

// 计算数字各位之和
int sum_of_digits(int n) {
    if(n == 0) {
        return 0;
    } else {
        return (n % 10) + sum_of_digits(n / 10);
    }
}

// 计算a的b次方
int power(int a, int b) {
    if(b == 0) {
        return 1;
    } else {
        return a * power(a, b - 1);
    }
}

int main() {
    int num = 123;
    printf("数字%d的各位之和: %d\n", num, sum_of_digits(num));
    
    int base = 2, exponent = 5;
    printf("%d的%d次方: %d\n", base, exponent, power(base, exponent));
    
    return 0;
}
```

**练习3答案：**
```c
#include <stdio.h>

// 计算数组和
int array_sum(int arr[], int size) {
    int sum = 0;
    for(int i = 0; i < size; i++) {
        sum += arr[i];
    }
    return sum;
}

// 找最大值和最小值
void find_max_min(int arr[], int size, int *max, int *min) {
    *max = *min = arr[0];
    for(int i = 1; i < size; i++) {
        if(arr[i] > *max) *max = arr[i];
        if(arr[i] < *min) *min = arr[i];
    }
}

// 冒泡排序
void bubble_sort(int arr[], int size) {
    for(int i = 0; i < size - 1; i++) {
        for(int j = 0; j < size - i - 1; j++) {
            if(arr[j] > arr[j + 1]) {
                int temp = arr[j];
                arr[j] = arr[j + 1];
                arr[j + 1] = temp;
            }
        }
    }
}

int main() {
    int arr[] = {64, 34, 25, 12, 22, 11, 90};
    int size = sizeof(arr) / sizeof(arr[0]);
    int sum, max, min;
    
    // 测试数组和
    sum = array_sum(arr, size);
    printf("数组和: %d\n", sum);
    
    // 测试找最大最小值
    find_max_min(arr, size, &max, &min);
    printf("最大值: %d, 最小值: %d\n", max, min);
    
    // 测试排序
    printf("排序前: ");
    for(int i = 0; i < size; i++) {
        printf("%d ", arr[i]);
    }
    printf("\n");
    
    bubble_sort(arr, size);
    
    printf("排序后: ");
    for(int i = 0; i < size; i++) {
        printf("%d ", arr[i]);
    }
    printf("\n");
    
    return 0;
}
```

---

## 第9章：指针入门

### 9.1 什么是指针？

指针是一个变量，它存储的是另一个变量的内存地址，而不是值本身。可以把指针想象成一张写着地址的纸条，通过这张纸条可以找到实际的房间（变量）。

### 9.2 指针的基本概念

**内存地址：**
每个变量在内存中都有一个地址，可以通过 `&` 运算符获取。

```c
#include <stdio.h>

int main() {
    int age = 25;
    
    // 打印变量的值和地址
    printf("age的值: %d\n", age);
    printf("age的地址: %p\n", &age);
    
    return 0;
}
```

**输出示例（M1架构）：**
```
age的值: 25
age的地址: 0x16fdff0a4
```

### 9.3 指针的声明和使用

**声明指针：**
```c
data_type *pointer_name;
```

**示例1：基本指针操作**
```c
#include <stdio.h>

int main() {
    int num = 10;
    int *ptr;  // 声明一个指向int类型的指针
    
    ptr = &num;  // 将num的地址赋给指针
    
    printf("num的值: %d\n", num);
    printf("num的地址: %p\n", &num);
    printf("ptr存储的地址: %p\n", ptr);
    printf("ptr指向的值: %d\n", *ptr);  // 使用*解引用
    
    // 通过指针修改变量的值
    *ptr = 20;
    printf("\n通过指针修改后:\n");
    printf("num的值: %d\n", num);
    printf("*ptr的值: %d\n", *ptr);
    
    return 0;
}
```

**重要符号：**
- `&`：取地址符，获取变量的地址
- `*`：解引用符，获取指针指向的值

### 9.4 指针和数组

数组名本质上是一个指向数组首元素的指针。

**示例1：数组和指针的关系**
```c
#include <stdio.h>

int main() {
    int arr[5] = {10, 20, 30, 40, 50};
    int *ptr = arr;  // 数组名就是首地址
    
    printf("数组访问:\n");
    printf("arr[0] = %d\n", arr[0]);
    printf("arr[1] = %d\n", arr[1]);
    
    printf("\n指针访问:\n");
    printf("*ptr = %d\n", *ptr);        // 第一个元素
    printf("*(ptr+1) = %d\n", *(ptr+1));  // 第二个元素
    printf("*(ptr+2) = %d\n", *(ptr+2));  // 第三个元素
    
    // 使用指针遍历数组
    printf("\n使用指针遍历数组: ");
    for(int i = 0; i < 5; i++) {
        printf("%d ", *(ptr + i));
    }
    printf("\n");
    
    return 0;
}
```

**示例2：指针算术运算**
```c
#include <stdio.h>

int main() {
    int arr[5] = {100, 200, 300, 400, 500};
    int *ptr = arr;
    
    printf("指针算术运算:\n");
    printf("ptr = %p, *ptr = %d\n", ptr, *ptr);
    
    ptr++;  // 移动到下一个int元素
    printf("ptr++后:\n");
    printf("ptr = %p, *ptr = %d\n", ptr, *ptr);
    
    ptr += 2;  // 再移动两个元素
    printf("ptr += 2后:\n");
    printf("ptr = %p, *ptr = %d\n", ptr, *ptr);
    
    return 0;
}
```

### 9.5 指针作为函数参数

使用指针可以在函数内修改外部变量的值。

**示例：交换两个数的值**
```c
#include <stdio.h>

// 交换函数 - 使用指针
void swap(int *a, int *b) {
    int temp = *a;
    *a = *b;
    *b = temp;
}

int main() {
    int x = 10, y = 20;
    
    printf("交换前: x = %d, y = %d\n", x, y);
    
    swap(&x, &y);  // 传递地址
    
    printf("交换后: x = %d, y = %d\n", x, y);
    
    return 0;
}
```

### 9.6 M1 ARM架构下的指针地址特点

在M1 ARM架构下，指针有一些特殊的特点：

```c
#include <stdio.h>

int main() {
    printf("=== M1 ARM架构下的指针特点 ===\n\n");
    
    // 测试不同数据类型的指针
    char ch = 'A';
    int num = 100;
    float f = 3.14;
    double d = 2.71828;
    
    char *ch_ptr = &ch;
    int *num_ptr = &num;
    float *f_ptr = &f;
    double *d_ptr = &d;
    
    printf("数据类型大小:\n");
    printf("char: %lu 字节\n", sizeof(char));
    printf("int: %lu 字节\n", sizeof(int));
    printf("float: %lu 字节\n", sizeof(float));
    printf("double: %lu 字节\n", sizeof(double));
    printf("指针: %lu 字节\n\n", sizeof(void *));
    
    printf("变量地址:\n");
    printf("ch地址: %p\n", ch_ptr);
    printf("num地址: %p\n", num_ptr);
    printf("f地址: %p\n", f_ptr);
    printf("d地址: %p\n\n", d_ptr);
    
    // 指针运算
    printf("指针运算:\n");
    printf("num_ptr = %p\n", num_ptr);
    printf("num_ptr + 1 = %p (相差%d字节)\n", num_ptr + 1, (int)((char *)(num_ptr + 1) - (char *)num_ptr));
    
    return 0;
}
```

**M1 ARM架构特点：**
- 所有指针类型都占用8字节（64位）
- 内存地址是64位的
- 指针算术运算会根据指向的数据类型自动调整

### 9.7 指针和字符串

字符串在C语言中是以 `\0` 结尾的字符数组。

```c
#include <stdio.h>

int main() {
    char str[] = "Hello M1";
    char *ptr = str;
    
    printf("字符串: %s\n", str);
    printf("使用指针访问: ");
    
    while(*ptr != '\0') {
        printf("%c ", *ptr);
        ptr++;
    }
    printf("\n");
    
    // 计算字符串长度
    ptr = str;
    int length = 0;
    while(*ptr != '\0') {
        length++;
        ptr++;
    }
    printf("字符串长度: %d\n", length);
    
    return 0;
}
```

### 9.8 常见指针错误

**错误1：未初始化的指针**
```c
int *ptr;  // 危险！指针未初始化
*ptr = 10; // 可能导致程序崩溃

// 正确做法
int num = 10;
int *ptr = &num;
```

**错误2：空指针解引用**
```c
int *ptr = NULL;
*ptr = 10;  // 错误！空指针不能解引用

// 正确做法
if(ptr != NULL) {
    *ptr = 10;
}
```

**错误3：指针越界**
```c
int arr[5];
int *ptr = arr;
ptr += 10;  // 危险！超出了数组范围
*ptr = 100; // 可能导致程序崩溃
```

### 9.9 课后练习

**练习1：指针基础**
```c
// 1. 声明一个int变量和一个指向它的指针
// 2. 使用指针修改变量的值
// 3. 打印变量的地址和值
// 4. 打印指针存储的地址和指向的值
```

**练习2：数组和指针**
```c
// 1. 声明一个包含5个元素的int数组
// 2. 使用指针遍历数组并打印所有元素
// 3. 使用指针找到数组中的最大值
```

**练习3：指针作为函数参数**
```c
// 编写一个函数，使用指针参数实现：
// 1. 计算两个数的和、差、积、商
// 2. 通过指针参数返回多个结果
```

**练习答案：**

**练习1答案：**
```c
#include <stdio.h>

int main() {
    int num = 25;
    int *ptr = &num;
    
    printf("num的值: %d\n", num);
    printf("num的地址: %p\n", &num);
    printf("ptr存储的地址: %p\n", ptr);
    printf("ptr指向的值: %d\n", *ptr);
    
    *ptr = 50;
    printf("\n修改后:\n");
    printf("num的值: %d\n", num);
    printf("*ptr的值: %d\n", *ptr);
    
    return 0;
}
```

**练习2答案：**
```c
#include <stdio.h>

int main() {
    int arr[5] = {10, 25, 5, 30, 15};
    int *ptr = arr;
    int max = arr[0];
    
    printf("数组元素: ");
    for(int i = 0; i < 5; i++) {
        printf("%d ", *(ptr + i));
        if(*(ptr + i) > max) {
            max = *(ptr + i);
        }
    }
    printf("\n最大值: %d\n", max);
    
    return 0;
}
```

**练习3答案：**
```c
#include <stdio.h>

void calculate(int a, int b, int *sum, int *difference, int *product, float *quotient) {
    *sum = a + b;
    *difference = a - b;
    *product = a * b;
    if(b != 0) {
        *quotient = (float)a / b;
    } else {
        *quotient = 0;  // 避免除以0
    }
}

int main() {
    int a = 10, b = 3;
    int sum, diff, prod;
    float quot;
    
    calculate(a, b, &sum, &diff, &prod, &quot);
    
    printf("%d + %d = %d\n", a, b, sum);
    printf("%d - %d = %d\n", a, b, diff);
    printf("%d × %d = %d\n", a, b, prod);
    printf("%d ÷ %d = %.2f\n", a, b, quot);
    
    return 0;
}
```

---

## 第10章：综合实战项目 - 简易学生成绩管理系统

### 10.1 项目介绍

我们将开发一个完整的学生成绩管理系统，综合运用前面学到的所有知识：变量、数组、函数、指针、循环、分支等。

**项目功能：**
- 添加学生信息
- 显示所有学生
- 查询学生成绩
- 计算平均分
- 成绩排序
- 统计及格率

### 10.2 完整代码

```c
/*
 * 学生成绩管理系统
 * 适用于 M1 MacBook Pro
 * 编译命令: gcc-14 student_system.c -o student_system
 */

#include <stdio.h>
#include <string.h>

#define MAX_STUDENTS 100  // 最大学生数
#define MAX_NAME_LENGTH 50  // 姓名最大长度

// 学生结构体
typedef struct {
    int id;                           // 学号
    char name[MAX_NAME_LENGTH];       // 姓名
    float chinese;                    // 语文成绩
    float math;                       // 数学成绩
    float english;                    // 英语成绩
    float average;                    // 平均分
} Student;

// 全局变量
Student students[MAX_STUDENTS];
int student_count = 0;

// 函数声明
void show_menu();
void add_student();
void display_all_students();
void search_student();
void calculate_class_average();
void sort_by_average();
void show_statistics();
void clear_screen();

int main() {
    int choice;
    
    printf("=========================================\n");
    printf("    学生成绩管理系统 v1.0\n");
    printf("    专为 M1 MacBook Pro 优化\n");
    printf("=========================================\n\n");
    
    while(1) {
        show_menu();
        printf("请选择操作: ");
        scanf("%d", &choice);
        
        switch(choice) {
            case 1:
                add_student();
                break;
            case 2:
                display_all_students();
                break;
            case 3:
                search_student();
                break;
            case 4:
                calculate_class_average();
                break;
            case 5:
                sort_by_average();
                break;
            case 6:
                show_statistics();
                break;
            case 0:
                printf("\n感谢使用！再见！\n");
                return 0;
            default:
                printf("无效的选择！请重新输入。\n");
        }
        
        printf("\n按回车键继续...");
        getchar();  // 吸收换行符
        getchar();  // 等待用户按键
        clear_screen();
    }
    
    return 0;
}

// 显示菜单
void show_menu() {
    printf("\n========== 主菜单 ==========\n");
    printf("1. 添加学生信息\n");
    printf("2. 显示所有学生\n");
    printf("3. 查询学生成绩\n");
    printf("4. 计算班级平均分\n");
    printf("5. 按平均分排序\n");
    printf("6. 成绩统计\n");
    printf("0. 退出系统\n");
    printf("=============================\n");
}

// 添加学生
void add_student() {
    if(student_count >= MAX_STUDENTS) {
        printf("学生数量已达上限！\n");
        return;
    }
    
    Student *s = &students[student_count];
    
    printf("\n--- 添加学生 ---\n");
    
    printf("请输入学号: ");
    scanf("%d", &s->id);
    
    printf("请输入姓名: ");
    scanf("%s", s->name);
    
    printf("请输入语文成绩: ");
    scanf("%f", &s->chinese);
    
    printf("请输入数学成绩: ");
    scanf("%f", &s->math);
    
    printf("请输入英语成绩: ");
    scanf("%f", &s->english);
    
    // 计算平均分
    s->average = (s->chinese + s->math + s->english) / 3.0;
    
    student_count++;
    
    printf("\n✅ 学生 %s 添加成功！\n", s->name);
    printf("平均分: %.2f\n", s->average);
}

// 显示所有学生
void display_all_students() {
    if(student_count == 0) {
        printf("\n暂无学生信息！\n");
        return;
    }
    
    printf("\n--- 所有学生信息 ---\n");
    printf("%-8s %-12s %-8s %-8s %-8s %-8s\n", 
           "学号", "姓名", "语文", "数学", "英语", "平均分");
    printf("--------------------------------------------------\n");
    
    for(int i = 0; i < student_count; i++) {
        printf("%-8d %-12s %-8.1f %-8.1f %-8.1f %-8.2f\n",
               students[i].id,
               students[i].name,
               students[i].chinese,
               students[i].math,
               students[i].english,
               students[i].average);
    }
    
    printf("--------------------------------------------------\n");
    printf("总人数: %d\n", student_count);
}

// 查询学生
void search_student() {
    if(student_count == 0) {
        printf("\n暂无学生信息！\n");
        return;
    }
    
    int search_id;
    int found = 0;
    
    printf("\n--- 查询学生 ---\n");
    printf("请输入学号: ");
    scanf("%d", &search_id);
    
    for(int i = 0; i < student_count; i++) {
        if(students[i].id == search_id) {
            printf("\n找到学生！\n");
            printf("学号: %d\n", students[i].id);
            printf("姓名: %s\n", students[i].name);
            printf("语文: %.1f\n", students[i].chinese);
            printf("数学: %.1f\n", students[i].math);
            printf("英语: %.1f\n", students[i].english);
            printf("平均分: %.2f\n", students[i].average);
            
            // 判断等级
            if(students[i].average >= 90) {
                printf("等级: 优秀 🌟\n");
            } else if(students[i].average >= 80) {
                printf("等级: 良好 👍\n");
            } else if(students[i].average >= 70) {
                printf("等级: 中等 😊\n");
            } else if(students[i].average >= 60) {
                printf("等级: 及格 📝\n");
            } else {
                printf("等级: 不及格 ⚠️\n");
            }
            
            found = 1;
            break;
        }
    }
    
    if(!found) {
        printf("\n❌ 未找到学号为 %d 的学生！\n", search_id);
    }
}

// 计算班级平均分
void calculate_class_average() {
    if(student_count == 0) {
        printf("\n暂无学生信息！\n");
        return;
    }
    
    float total_chinese = 0, total_math = 0, total_english = 0;
    float avg_chinese, avg_math, avg_english;
    
    for(int i = 0; i < student_count; i++) {
        total_chinese += students[i].chinese;
        total_math += students[i].math;
        total_english += students[i].english;
    }
    
    avg_chinese = total_chinese / student_count;
    avg_math = total_math / student_count;
    avg_english = total_english / student_count;
    
    printf("\n--- 班级平均分 ---\n");
    printf("语文平均分: %.2f\n", avg_chinese);
    printf("数学平均分: %.2f\n", avg_math);
    printf("英语平均分: %.2f\n", avg_english);
    printf("总平均分: %.2f\n", (avg_chinese + avg_math + avg_english) / 3.0);
}

// 按平均分排序
void sort_by_average() {
    if(student_count == 0) {
        printf("\n暂无学生信息！\n");
        return;
    }
    
    // 使用冒泡排序
    for(int i = 0; i < student_count - 1; i++) {
        for(int j = 0; j < student_count - i - 1; j++) {
            if(students[j].average < students[j + 1].average) {
                // 交换学生信息
                Student temp = students[j];
                students[j] = students[j + 1];
                students[j + 1] = temp;
            }
        }
    }
    
    printf("\n✅ 已按平均分从高到低排序！\n");
    display_all_students();
}

// 成绩统计
void show_statistics() {
    if(student_count == 0) {
        printf("\n暂无学生信息！\n");
        return;
    }
    
    int pass_count = 0;
    int excellent_count = 0;
    float max_average = students[0].average;
    float min_average = students[0].average;
    int max_index = 0, min_index = 0;
    
    for(int i = 0; i < student_count; i++) {
        // 统计及格人数
        if(students[i].average >= 60) {
            pass_count++;
        }
        
        // 统计优秀人数
        if(students[i].average >= 90) {
            excellent_count++;
        }
        
        // 找最高和最低分
        if(students[i].average > max_average) {
            max_average = students[i].average;
            max_index = i;
        }
        if(students[i].average < min_average) {
            min_average = students[i].average;
            min_index = i;
        }
    }
    
    float pass_rate = (float)pass_count / student_count * 100;
    float excellent_rate = (float)excellent_count / student_count * 100;
    
    printf("\n--- 成绩统计 ---\n");
    printf("总人数: %d\n", student_count);
    printf("及格人数: %d\n", pass_count);
    printf("优秀人数: %d\n", excellent_count);
    printf("及格率: %.2f%%\n", pass_rate);
    printf("优秀率: %.2f%%\n", excellent_rate);
    printf("\n最高分:\n");
    printf("  学生: %s (%.2f分)\n", students[max_index].name, max_average);
    printf("\n最低分:\n");
    printf("  学生: %s (%.2f分)\n", students[min_index].name, min_average);
}

// 清屏函数
void clear_screen() {
    // 在终端中清屏
    printf("\033[2J\033[H");
}
```

### 10.3 M1 MBP编译运行步骤

**步骤1：保存代码**
将上述代码保存为 `student_system.c`

**步骤2：打开终端**
打开终端，进入文件所在目录

**步骤3：编译程序**
```bash
# 使用GCC编译器
gcc-14 student_system.c -o student_system

# 或者使用Clang（macOS自带）
clang student_system.c -o student_system
```

**步骤4：运行程序**
```bash
./student_system
```

**步骤5：测试功能**
按照菜单提示测试各个功能

### 10.4 代码详解

**结构体定义：**
```c
typedef struct {
    int id;
    char name[MAX_NAME_LENGTH];
    float chinese;
    float math;
    float english;
    float average;
} Student;
```

**主要函数：**
- `add_student()`：添加学生信息
- `display_all_students()`：显示所有学生
- `search_student()`：按学号查询学生
- `calculate_class_average()`：计算班级平均分
- `sort_by_average()`：按平均分排序
- `show_statistics()`：显示成绩统计

### 10.5 扩展功能建议

你可以尝试添加以下功能：

1. **数据持久化**：将数据保存到文件
2. **删除学生**：根据学号删除学生
3. **修改成绩**：修改已存在学生的成绩
4. **科目统计**：分别统计各科成绩
5. **图形化界面**：使用图形库创建GUI

### 10.6 课后练习

**练习1：添加删除功能**
```c
// 在系统中添加删除学生的功能
// 根据学号删除学生
```

**练习2：数据保存**
```c
// 将学生信息保存到文件中
// 下次运行时可以从文件读取
```

**练习3：成绩分析**
```c
// 添加更详细的成绩分析功能
// 如：各科成绩分布、标准差等
```

---

## 附录A：M1 MacBook Pro开发环境常见问题

### 问题1：gcc命令未找到

```bash
# 解决方案1：使用完整路径
/opt/homebrew/bin/gcc-14 --version

# 解决方案2：创建别名
echo 'alias gcc="gcc-14"' >> ~/.zshrc
source ~/.zshrc
```

### 问题2：VS Code无法编译

```bash
# 检查tasks.json中的gcc路径
# 确保使用的是/opt/homebrew/bin/gcc-14
```

### 问题3：头文件找不到

```bash
# 安装Xcode命令行工具
xcode-select --install
```

### 问题4：程序运行权限问题

```bash
# 给程序添加执行权限
chmod +x your_program
```

---

## 附录B：C语言学习路线图

### 初级阶段（第1-4章）
- ✅ 环境搭建
- ✅ 第一个程序
- ✅ 变量和数据类型
- ✅ 运算符和表达式

### 中级阶段（第5-7章）
- ✅ 分支结构
- ✅ 循环结构
- ✅ 数组

### 高级阶段（第8-10章）
- ✅ 函数
- ✅ 指针
- ✅ 综合项目

### 后续学习方向
- 📚 结构体和联合体
- 📚 文件操作
- 📚 动态内存分配
- 📚 数据结构（链表、栈、队列）
- 📚 算法（排序、搜索）

---

## 附录C：Xcode集成开发环境使用指南

### 安装Xcode

1. 打开App Store
2. 搜索"Xcode"
3. 点击安装（约12GB，需要耐心等待）

### 创建C语言项目

1. 打开Xcode
2. File → New → Project
3. 选择macOS → Command Line Tool
4. 填写项目信息：
   - Product Name: 你的项目名称
   - Team: 选择你的Apple ID
   - Organization Name: 你的组织名称
   - Organization Identifier: 如 com.yourname
   - Language: 选择 C

### 编写和运行程序

1. 在左侧导航栏找到 `main.c`
2. 编写你的C代码
3. 点击左上角的播放按钮运行
4. 在下方控制台查看输出

### 调试技巧

1. **设置断点**：点击代码行号左侧
2. **单步执行**：使用调试工具栏
3. **查看变量**：在变量视图中查看
4. **控制台输出**：使用 `printf()` 调试

### Xcode快捷键

- `Cmd + R`：运行程序
- `Cmd + B`：编译程序
- `Cmd + .`：停止运行
- `Cmd + /`：注释/取消注释
- `Cmd + [`：左缩进
- `Cmd + ]`：右缩进

---

## 结语

恭喜你完成了这份C语言零基础教程！你已经掌握了：

✅ C语言的基础语法
✅ M1 MacBook Pro的开发环境配置
✅ 变量、数据类型、运算符
✅ 分支和循环结构
✅ 数组和函数
✅ 指针的基本使用
✅ 完整项目的开发

现在你可以：
1. 继续深入学习C语言的高级特性
2. 学习其他编程语言（如Swift）
3. 开始开发自己的项目

记住：编程是一门实践性很强的技能，多写代码、多思考、多总结，你一定会成为一名优秀的程序员！

加油！🚀

---

**教程编写：专为M1 MacBook Pro用户设计**  
**最后更新：2025年**  
**配套代码：每章都提供完整示例代码**

